name: geotgr
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.12
  - pip
  - numpy
  - pandas
  - scipy
  - matplotlib
  - multiprocess
  - tqdm
  - astropy
  - ipykernel
  - jax
  - jaxlib
  - qnm
  - spherical
  - quaternionic
  - gwpopulation
  - pathos
  - lal
  - pip:
    - jimGW
    - pycbc
    # - lal

# conda env create -f geotgr.yml
# conda env update --file geotgr.yml --prune
# conda remove --name geotgr --all